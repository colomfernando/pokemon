import React from 'react';

interface PropsStat {
  name: string;
  value: number;
}

const PokemonStat: React.FC<PropsStat> = ({ name, value }) => {
  return <div> </div>;
};

export default PokemonStat;
